# 只列实际 import 的直接依赖,版本锁死当前安装版(transitive 如 torch 不列)。
# 全部按"实际 import 时才加"节奏增量入栈,transitive 不列。
langgraph==1.2.0
duckdb==1.5.2
pandas==3.0.3
numpy==2.4.5
mcp==1.27.1
pytest==9.0.3
# 阶段 4a RAG:BGE-M3 向量召回 + bge-reranker-v2-m3 重排 + Chroma + 切块
sentence-transformers==5.5.0
chromadb==1.5.9
langchain-text-splitters==1.1.2
# 阶段 4b Memory:Mem0 商家画像 + A.5 确定性写入(infer=False);LLM/embedder/vector 全复用 4a 锁定栈
mem0ai[nlp]==2.0.2
# 阶段 5 可观测:LangSmith trace(@traceable 显式装饰,LangGraph 1.x 需挂载点激活 callback 链)
langsmith==0.8.5
# 阶段 5 UI:Streamlit demo
streamlit==1.57.0
# v2 持久化：原生 SQL + psycopg3 + 官方 LangGraph Postgres checkpointer
psycopg[binary,pool]==3.3.4
langgraph-checkpoint-postgres==3.1.0
# v2 HTTP 服务边界
fastapi==0.141.1
uvicorn==0.47.0
# v3 声明式 Skill DSL；safe_load 只解析数据，不构造可执行对象
PyYAML==6.0.3
certifi==2026.7.22
