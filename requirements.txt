# 只列实际 import 的直接依赖,版本锁死当前安装版(transitive 如 torch 不列)。
# mem0 留到阶段 4b 真用时再加,不预填。
langgraph==1.2.0
duckdb==1.5.2
pandas==3.0.3
numpy==2.4.5
mcp==1.27.1
pytest==9.0.3
# 阶段 4a RAG:BGE-M3 向量召回 + bge-reranker-v2-m3 重排 + Chroma + 切块
sentence-transformers==5.5.0
chromadb==1.5.9
langchain-text-splitters==1.1.2
