# Cross-period Comparison

Use this Skill only when at least two explicit periods can be compared. Run the same
read-only metric action for both windows and preserve each tool result. Expose partial
or unequal windows instead of normalizing them silently. Absolute and relative changes
must be computed from the returned structured values by deterministic code.

If either period has no evidence, stop and label the comparison incomplete.
